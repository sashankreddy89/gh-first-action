name: First Workflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print greeting
        run: echo "Hello World"
      - name: Print goodbye
        run: echo "Done - Bye!"
  second-job:
    runs-on: ubuntu-latest
    steps:
      - name: Multi Line
        run: |
          echo "Success"
          false
          echo "Won't Execute"
